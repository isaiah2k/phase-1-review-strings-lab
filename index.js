// Variable to store the name of the current user
const currentUser = 'Grace Hopper!'
// Variable to store a welcome message for the current user
const welcomeMessage = `Welcome to Flatbook, ${currentUser}!`
// Variable to store an excited welcome message in all uppercase letters
const excitedWelcomeMessage = welcomeMessage.toUpperCase()
// Variable to store a short greeting that includes only the first letter of the current user's name
const shortGreeting = `Welcome, ${currentUser.slice(0, 1)}!`